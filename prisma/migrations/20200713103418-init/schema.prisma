generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url = "***"
}

model user {
  email String  @unique
  id    Int     @default(autoincrement()) @id
  name  String?
}
